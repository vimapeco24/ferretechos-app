{
  "name": "charcuteria-app",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo: backend/ (API) + frontend/ (React PWA)",
  "scripts": {
    "dev:backend": "cd backend && SUPABASE_URL=$SUPABASE_URL SUPABASE_ANON_KEY=$SUPABASE_ANON_KEY node index.js",
    "dev:frontend": "cd frontend && npm start",
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build"
  }
}
